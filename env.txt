el archivo .env sirve para guardar una variable con la direccion index de la restapi es una variable que ayudara para
 no tener que cambiar mucho la direcciones en las lineas de codigo solo sera en el documento su estructura para mas facilidad
 es "REACT_API_" + <el nombre que quieras>